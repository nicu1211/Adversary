import React,{useEffect,useMemo,useState}from'react';
import{AreaChart,Area,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid}from'recharts';

const NL=String.fromCharCode(10),LK='bdo_logs_v10',MK='bdo_members_v10';
const API='';
const TK='bdo_admin_token';

function adminToken(){
  let t=localStorage.getItem(TK);
  if(!t){
    t=prompt('Admin token pentru salvare/stergere loguri:')||'';
    if(t)localStorage.setItem(TK,t);
  }
  return t;
}

async function apiGet(path){
  const r=await fetch(API+path);
  if(!r.ok)throw new Error(await r.text());
  return r.json();
}

async function apiWrite(path,method,body){
  const r=await fetch(API+path,{
    method,
    headers:{
      'Content-Type':'application/json',
      'x-admin-token':adminToken()
    },
    body:body?JSON.stringify(body):undefined
  });
  if(r.status===401){
    localStorage.removeItem(TK);
    throw new Error('Admin token gresit');
  }
  if(!r.ok)throw new Error(await r.text());
  return r.json();
}

const demo=`[08:03:46] Vollkornbeet has killed Tarei from Apex (KornSeric,Shin_Tarei)
[08:03:46] GoldFireNOR has killed TheFarPlane from Apex (Vipps,Asphyxia_Somnia)
[08:03:49] Jonah has killed Goof0 from Apex (Pryz,Thesyra)
[08:04:51] Kawoy has killed LordAizen from Apex (Kawoy,One_King)
[08:05:22] Kawoy has killed Ambulance from Apex (Kawoy,Svrgery)
[08:07:32] Kawoy has killed FairyGuild from Nightingale (Kawoy,Jinwoo_HAN)
[08:08:30] OG_Hege died to Kanchi from Nightingale (Giantboi5000,Zoritoshi)
[08:10:44] Jonah died to ImDrover from Nightingale (Pryz,MeneerDeJoekel)
[08:16:33] Kawoy died to Syrice from LosGalacticos (Kawoy,SyriceEU)
[08:20:31] TheWuffs has killed Einherjarx31 from LosGalacticos (Deafear,AFYOK59)`;

const ach=[['100 Kills',100,'k'],['500 Kills',500,'k'],['1000 Kills',1000,'k'],['K/D 2+',2,'kd'],['K/D 5+',5,'kd'],['5 Killstreak',5,'s'],['10 Killstreak',10,'s'],['5 KillFeed',5,'f']];
const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,mon=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,today=()=>iso(new Date()),mLabel=m=>{let[a,b]=m.split('-').map(Number);return new Date(a,b-1,1).toLocaleString('en-US',{month:'long',year:'numeric'})},shiftM=(m,n)=>{let[a,b]=m.split('-').map(Number);return mon(new Date(a,b-1+n,1))},mDays=m=>{let[a,b]=m.split('-').map(Number),f=new Date(a,b-1,1),s=new Date(a,b-1,1-f.getDay());return Array.from({length:42},(_,i)=>{let d=new Date(s);d.setDate(s.getDate()+i);return{iso:iso(d),day:d.getDate(),cur:d.getMonth()===b-1}})},rd=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f))}catch{return f}},wr=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),add=(o,k,n=1)=>o[k]=(o[k]||0)+n,sec=t=>{let p=t.split(':').map(Number);return p[0]*3600+p[1]*60+p[2]},tm=s=>String(Math.floor(s/3600)).padStart(2,'0')+':'+String(Math.floor(s/60)%60).padStart(2,'0'),clean=s=>String(s||'').split(NL).map(x=>x.trim()).filter(Boolean).join(NL),hash=s=>{let h=0,t=clean(s);for(let c of t)h=((h<<5)-h+c.charCodeAt(0))|0;return''+h},dateOf=x=>x.date||today();

function parse(raw,name,date,id){return clean(raw).split(NL).map((line,i)=>{let c=line.indexOf(']'),ps=line.lastIndexOf('('),pe=line.lastIndexOf(')');if(c<0||ps<0||pe<0)return null;let time=line.slice(1,c),info=line.slice(c+2,ps).trim(),f=line.slice(ps+1,pe).split(',');if(f.length<2)return null;if(info.includes(' has killed ')){let[a,r]=info.split(' has killed '),[v,g]=r.split(' from ');return g&&{i,type:'kill',time,sec:sec(time),killer:a,victim:v,guild:g,kf:f[0],vf:f[1],war:name,date,id}}if(info.includes(' died to ')){let[v,r]=info.split(' died to '),[k,g]=r.split(' from ');return g&&{i,type:'death',time,sec:sec(time),killer:k,victim:v,guild:g,kf:f[1],vf:f[0],war:name,date,id}}return null}).filter(Boolean).sort((a,b)=>a.sec-b.sec||a.i-b.i)}
function streaks(e){let cur={},best={};e.forEach(x=>{if(x.type==='kill'){cur[x.killer]=(cur[x.killer]||0)+1;best[x.killer]=Math.max(best[x.killer]||0,cur[x.killer])}else cur[x.victim]=0});return best}
function feed(e,w=10,details=false){let by={};e.filter(x=>x.type==='kill').forEach(x=>{let k=x.killer+'@@'+x.id;(by[k]||=[]).push(x)});let out=details?[]:{};for(let[k,a]of Object.entries(by)){a.sort((x,y)=>x.sec-y.sec);let n=k.split('@@')[0],l=0,bs=0,be=0;for(let r=0;r<a.length;r++){while(a[r].sec-a[l].sec>w)l++;if(r-l>be-bs){bs=l;be=r}}let list=a.slice(bs,be+1);if(details){list.length>1&&out.push({name:n,count:list.length,start:list[0].time,end:list.at(-1).time,war:list[0].war,date:list[0].date,id:list[0].id,victims:list.map(x=>x.victim)})}else out[n]=Math.max(out[n]||0,list.length)}return details?out.sort((a,b)=>b.count-a.count||a.date.localeCompare(b.date)):out}
function stats(items){let ev=items.flatMap(x=>parse(x.raw,x.name,x.date,x.id)).sort((a,b)=>a.date.localeCompare(b.date)||a.sec-b.sec);if(!ev.length)return{ev:[],players:[],guilds:[],line:[],kills:0,deaths:0,kd:'0.00'};let pk={},pd={},fam={},gk={},gd={},mins={};ev.forEach(x=>{x.type==='kill'?add(pk,x.killer):add(pd,x.victim);add(gk,x.guild,x.type==='kill'?1:0);add(gd,x.guild,x.type==='death'?1:0);fam[x.killer]=x.kf;fam[x.victim]=x.vf;let m=tm(Math.floor(x.sec/60)*60);mins[m]||={time:m,kills:0,deaths:0};mins[m][x.type==='kill'?'kills':'deaths']++});let first=Math.min(...ev.map(x=>x.sec)),last=Math.max(...ev.map(x=>x.sec)),line=[];for(let t=Math.floor(first/60)*60;t<=Math.floor(last/60)*60;t+=60)line.push(mins[tm(t)]||{time:tm(t),kills:0,deaths:0});let players=[...new Set([...Object.keys(pk),...Object.keys(pd)])].map(n=>{let k=pk[n]||0,d=pd[n]||0;return{name:n,family:fam[n]||'-',kills:k,deaths:d,kd:d?(k/d).toFixed(2):k.toFixed(2)}}).sort((a,b)=>b.kills-a.kills||a.deaths-b.deaths);let guilds=[...new Set([...Object.keys(gk),...Object.keys(gd)])].map(n=>{let k=gk[n]||0,d=gd[n]||0;return{name:n,kills:k,deaths:d,kd:d?(k/d).toFixed(2):k.toFixed(2)}});let k=ev.filter(x=>x.type==='kill').length,d=ev.filter(x=>x.type==='death').length;return{ev,players,guilds,line,kills:k,deaths:d,kd:d?(k/d).toFixed(2):k.toFixed(2),st:streaks(ev),fd:feed(ev)}}
const Panel=({children,cls=''})=><section className={`rounded-3xl border border-slate-700 bg-slate-950/70 p-5 shadow-2xl ${cls}`}>{children}</section>,Metric=({i,l,v,s,c})=><div className={`rounded-2xl border p-5 bg-gradient-to-br to-slate-950/40 ${c}`}><div className="flex gap-4"><b className="text-4xl">{i}</b><div><p className="text-sm text-slate-300">{l}</p><p className="text-3xl font-black">{v}</p><p className="text-sm text-slate-400">{s}</p></div></div></div>;
function Cal({m,setM,selected,marked,onPick,footer}){return <div className="rounded-2xl border border-slate-700 bg-slate-950 p-3 shadow-2xl"><div className="mb-3 flex items-center justify-between"><button onClick={()=>setM(shiftM(m,-1))} className="rounded-lg border border-slate-700 px-2 py-1 hover:bg-slate-800">‹</button><b className="text-sm">{mLabel(m)}</b><button onClick={()=>setM(shiftM(m,1))} className="rounded-lg border border-slate-700 px-2 py-1 hover:bg-slate-800">›</button></div><div className="mb-2 grid grid-cols-7 gap-1 text-center text-[10px] font-black text-slate-500">{['Su','Mo','Tu','We','Th','Fr','Sa'].map(x=><span key={x}>{x}</span>)}</div><div className="grid grid-cols-7 gap-1">{mDays(m).map(d=><button key={d.iso} onClick={()=>onPick(d.iso)} className={`relative h-8 rounded-lg text-xs font-black transition ${selected===d.iso?'bg-blue-500 text-white ring-2 ring-blue-300':marked.has(d.iso)?'bg-emerald-500/20 text-emerald-200 hover:bg-emerald-500/35':d.cur?'text-slate-300 hover:bg-slate-800':'text-slate-600 hover:bg-slate-900'}`}>{d.day}{marked.has(d.iso)&&<span className="absolute bottom-1 left-1/2 h-1 w-1 -translate-x-1/2 rounded-full bg-emerald-300"/>}</button>)}</div>{footer}</div>}
function Best({players,members,st,fd,ev}){let[q,setQ]=useState(''),by=Object.fromEntries(players.map(p=>[p.name,p])),names=[...new Set([...(members||[]).map(m=>m.name),...players.map(p=>p.name)])],rows=names.map(name=>{let p=by[name]||{name,kills:0,deaths:0,kd:'0.00'};return{...p,kdN:+p.kd,streak:st[name]||0,feed:fd[name]||0}}),reach={},run={};[...(ev||[])].sort((a,b)=>a.date.localeCompare(b.date)||a.sec-b.sec||a.i-b.i).filter(e=>e.type==='kill').forEach(e=>{run[e.killer]=(run[e.killer]||0)+1;let final=by[e.killer]?.kills||0;if(final&&run[e.killer]===final)reach[e.killer]=e.date+' '+String(e.sec).padStart(5,'0')+' '+String(e.i).padStart(5,'0')}),rankKills=()=>Object.fromEntries([...rows].sort((a,b)=>b.kills-a.kills||(reach[a.name]||'9999').localeCompare(reach[b.name]||'9999')).map((p,i)=>[p.name,i+1])),rank=(k,desc=true)=>{let sorted=[...rows].sort((a,b)=>desc?b[k]-a[k]:a[k]-b[k]),out={},last,rankNo=0;sorted.forEach((p,i)=>{let v=Number(p[k])||0;if(i===0||v!==last)rankNo=i+1;out[p.name]=rankNo;last=v});return out},r={kills:rankKills(),deaths:rank('deaths',false),kd:rank('kdN'),streak:rank('streak'),feed:rank('feed')},final=rows.map(p=>({...p,avg:(r.kills[p.name]+r.deaths[p.name]+r.kd[p.name]+r.streak[p.name]+r.feed[p.name])/5})).filter(p=>p.name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>a.avg-b.avg);return <Panel cls="h-full"><h3 className="text-xl font-black">♛ Best Overall</h3><p className="mb-3 text-xs text-slate-400">Average rank across whole guild</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search player..." className="mb-3 w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2 text-sm outline-none focus:border-blue-400"/>{!final.length?<p className="text-slate-500">No players.</p>:<div className="max-h-[500px] space-y-1.5 overflow-y-auto pr-1 [scrollbar-width:thin] [scrollbar-color:#334155_transparent] [&::-webkit-scrollbar]:w-2 [&::-webkit-scrollbar-thumb]:rounded-full [&::-webkit-scrollbar-thumb]:bg-slate-700">{final.map((p,i)=><div key={p.name} className="rounded-xl border border-slate-800 bg-slate-900/70 p-2 hover:bg-slate-900"><div className="mb-1.5 flex justify-between gap-2"><b className="truncate"><span className="mr-2 text-slate-500">{i+1}</span>{p.name}</b><span className="rounded-md border border-blue-400/20 bg-blue-500/5 px-2 py-1 text-sm font-black text-blue-300"><small className="mr-1 text-[9px] uppercase text-blue-200/80">Avg</small>{p.avg.toFixed(2)}</span></div><div className="grid grid-cols-5 gap-1 text-center text-xs">{[['Kills',r.kills[p.name],'text-blue-300'],['Deaths',r.deaths[p.name],'text-pink-300'],['K/D',r.kd[p.name],'text-emerald-300'],['Streak',r.streak[p.name],'text-slate-200'],['Feed',r.feed[p.name],'text-orange-300']].map(x=><div key={x[0]} className="rounded-md bg-slate-950/70 p-1"><p className="text-slate-500">{x[0]}</p><b className={x[2]}>#{x[1]}</b></div>)}</div></div>)}</div>}</Panel>}
function Overview({players,st,fd,ev}){let[q,setQ]=useState(''),[sort,setSort]=useState(['kills','desc']),[sel,setSel]=useState(null),[key,dir]=sort,rows=players.map(p=>({...p,streak:st[p.name]||0,feed:fd[p.name]||0})).filter(p=>p.name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>{let av=key==='name'?a.name.toLowerCase():+a[key],bv=key==='name'?b.name.toLowerCase():+b[key];return av<bv?(dir==='asc'?-1:1):av>bv?(dir==='asc'?1:-1):0}),flip=k=>setSort(key===k?[k,dir==='desc'?'asc':'desc']:[k,k==='name'?'asc':'desc']),H=({id,children,cls=''})=><th className={'py-3 '+cls}><button onClick={()=>flip(id)} className={key===id?'text-blue-300 font-black':'font-black hover:text-blue-300'}>{children} {key===id?(dir==='desc'?'↓':'↑'):'↕'}</button></th>;let hist=sel?ev.filter(e=>e.killer===sel.name||e.victim===sel.name).sort((a,b)=>a.date.localeCompare(b.date)||a.sec-b.sec):[],kills=hist.filter(e=>e.killer===sel?.name).length,deaths=hist.filter(e=>e.victim===sel?.name).length,kd=deaths?(kills/deaths).toFixed(2):kills.toFixed(2),vict={},nem={};hist.forEach(e=>{if(e.killer===sel?.name)add(vict,e.victim);if(e.victim===sel?.name)add(nem,e.killer)});let fav=Object.entries(vict).sort((a,b)=>b[1]-a[1])[0]||['-',0],bad=Object.entries(nem).sort((a,b)=>b[1]-a[1])[0]||['-',0];return <Panel cls="h-full"><div className="mb-4 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h3 className="text-xl font-black">♙ Player Overview</h3><p className="text-xs text-slate-400">Click pe nume pentru istoricul killurilor</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search family name" className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-2 text-sm outline-none focus:border-blue-400 md:w-64"/></div><div className="overflow-hidden rounded-2xl border border-slate-800"><div className="max-h-[500px] overflow-y-auto pr-1 [scrollbar-width:thin] [scrollbar-color:#334155_transparent] [&::-webkit-scrollbar]:w-2 [&::-webkit-scrollbar-thumb]:rounded-full [&::-webkit-scrollbar-thumb]:bg-slate-700"><table className="w-full text-sm"><thead className="sticky top-0 z-10 bg-slate-900 text-xs uppercase text-slate-400"><tr><H id="name" cls="pl-4 text-left">Family</H><H id="kills" cls="text-right">Kills</H><H id="deaths" cls="text-right">Deaths</H><H id="kd" cls="text-right">K/D</H><H id="streak" cls="text-right">Killstreak</H><H id="feed" cls="pr-4 text-right">KillFeed</H></tr></thead><tbody>{rows.map(p=><tr key={p.name} className="border-t border-slate-800 bg-slate-950/30 hover:bg-slate-900/50"><td className="py-3 pl-4"><button onClick={()=>setSel(p)} className="rounded-full border border-cyan-400/30 bg-cyan-500/10 px-3 py-1 font-bold text-cyan-300 hover:border-cyan-300 hover:bg-cyan-500/20">{p.name}</button></td><td className="py-3 text-right font-black text-blue-300">⚔ {p.kills}</td><td className="py-3 text-right font-black text-pink-300">☠ {p.deaths}</td><td className="py-3 text-right font-black text-emerald-300">✺ {p.kd}</td><td className="py-3 text-right font-black">{p.streak}</td><td className="py-3 pr-4 text-right font-black text-orange-300">🔥 {p.feed}</td></tr>)}</tbody></table></div></div>{sel&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4 backdrop-blur-sm"><div className="max-h-[86vh] w-full max-w-5xl overflow-hidden rounded-3xl border border-slate-700 bg-slate-950 shadow-2xl"><div className="flex items-start justify-between gap-4 border-b border-slate-800 p-5"><div><h3 className="text-2xl font-black">{sel.name} highlights & history</h3><div className="mt-3 flex flex-wrap gap-2 text-sm"><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1">Kills <b className="text-blue-300">{kills}</b></span><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1">Deaths <b className="text-pink-300">{deaths}</b></span><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1">KD <b className="text-emerald-300">{kd}</b></span><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1">Killstreak <b>{st[sel.name]||0}</b></span><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1">Killfeed <b className="text-orange-300">{fd[sel.name]||0}</b></span></div></div><button onClick={()=>setSel(null)} className="rounded-xl border border-slate-700 bg-slate-900 px-4 py-2 font-bold hover:bg-slate-800">Close</button></div><div className="p-4"><div className="mb-4 grid gap-3 md:grid-cols-2"><div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><p className="text-xs font-bold uppercase text-slate-500">Favorite victim</p><p className="mt-1 font-black">{fav[0]}</p><p className="text-sm font-bold text-blue-300">{fav[1]} kills</p></div><div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><p className="text-xs font-bold uppercase text-slate-500">Nemesis</p><p className="mt-1 font-black">{bad[0]}</p><p className="text-sm font-bold text-pink-300">{bad[1]} deaths</p></div></div><div className="max-h-[48vh] overflow-auto rounded-2xl border border-slate-800"><table className="w-full text-sm"><thead className="sticky top-0 bg-slate-900 text-xs uppercase text-slate-400"><tr><th className="py-3 pl-4 text-left">Time</th><th className="py-3 text-left">Type</th><th className="py-3 text-left">Opponent</th><th className="py-3 pr-4 text-left">Guild / War</th></tr></thead><tbody>{hist.map((e,i)=><tr key={i} className="border-t border-slate-800 bg-slate-950/30"><td className="py-3 pl-4 font-black">{e.time}</td><td className="py-3"><span className={(e.killer===sel.name?'bg-blue-500/15 text-blue-300':'bg-pink-500/15 text-pink-300')+' rounded-full px-3 py-1 text-xs font-black'}>{e.killer===sel.name?'KILL':'DEATH'}</span></td><td className="py-3 font-bold">{e.killer===sel.name?e.victim:e.killer}</td><td className="py-3 pr-4 text-slate-400">{e.guild} / {e.war}</td></tr>)}</tbody></table></div></div></div></div>}</Panel>}
function RankList({title,items,valueKey}){let rows=items.slice(0,5),max=Math.max(1,...rows.map(x=>Number(x[valueKey])||0));return <Panel><h3 className="mb-4 text-xl font-black">{title}</h3>{!rows.length?<p className="text-slate-500">No data yet.</p>:rows.map((x,i)=>{let v=Number(x[valueKey])||0;return <div key={x.name} className="mb-4 grid grid-cols-[34px_1fr_55px] items-center gap-3 text-sm"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-slate-700 font-black">{i+1}</span><div className="min-w-0"><p className="mb-2 truncate font-bold">{x.name}</p><div className="h-2.5 rounded-full bg-slate-800"><div className="h-2.5 rounded-full bg-gradient-to-r from-blue-500 to-cyan-300" style={{width:Math.max(6,Math.round(v/max*100))+'%'}}/></div></div><b className="text-right">{v}</b></div>})}</Panel>}
function NodeWars({logs,setPage,setSelDays,setSelWars,selWars}){let[q,setQ]=useState(''),rows=logs.map(x=>{let s=stats([{...x,date:dateOf(x)}]),top=[...s.guilds].map(g=>{let ourKills=g.kills,ourDeaths=g.deaths,total=ourKills+ourDeaths,kd=ourKills?(ourDeaths/ourKills).toFixed(2):ourDeaths.toFixed(2);return{name:g.name,kills:ourDeaths,deaths:ourKills,total,kd}}).sort((a,b)=>b.total-a.total||b.kills-a.kills).slice(0,5);return{...x,date:dateOf(x),players:s.players.length,kills:s.kills,deaths:s.deaths,kd:s.kd,top}}).filter(r=>!q.trim()||r.top.some(g=>g.name.toLowerCase().includes(q.toLowerCase()))).sort((a,b)=>b.date.localeCompare(a.date));return <Panel><div className="mb-4 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="text-2xl font-black">Node Wars</h2><p className="text-sm text-slate-400">Istoricul meciurilor salvate · bifează mai multe pentru analiză în Overview</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search enemies..." className="w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2 text-sm outline-none transition placeholder:text-slate-500 focus:border-blue-400 focus:bg-slate-900 md:w-72"/></div><div className="overflow-hidden rounded-2xl border border-slate-800"><div className="max-h-[720px] overflow-auto"><table className="w-full min-w-[980px] text-sm"><thead className="sticky top-0 z-10 bg-slate-950 text-xs uppercase tracking-wider text-slate-400"><tr><th className="py-4 pl-4 text-left">Time ↕</th><th className="py-4 text-left">Alliance</th><th className="py-4 text-left">Top 5 enemies</th><th className="py-4 text-center">Players ↕</th><th className="py-4 text-center">Kills ↕</th><th className="py-4 text-center">Deaths ↕</th><th className="py-4 text-center">KD ↕</th><th className="py-4 pr-4 text-center">Select</th></tr></thead><tbody>{!rows.length?<tr><td colSpan="8" className="py-8 text-center text-slate-500">No saved node wars yet. Add logs from Raw Log.</td></tr>:rows.map(r=><tr key={r.id} onClick={()=>{setSelDays([r.date]);setSelWars([String(r.id)]);setPage('overview')}} className="cursor-pointer border-t border-slate-800 bg-slate-950/30 transition hover:bg-slate-900/60"><td className="py-4 pl-4 font-black text-slate-200">{new Date(r.date).toLocaleDateString('en-GB',{weekday:'short',day:'2-digit',month:'2-digit',year:'numeric'})}</td><td className="py-4"><span className="rounded-full border border-slate-700 bg-slate-900 px-3 py-1 text-xs font-bold">Adversary <b className={+r.kd>=1?'text-emerald-300':'text-rose-300'}>{r.kd}</b></span></td><td className="py-4"><div className="flex max-w-[460px] flex-wrap gap-1.5">{r.top.map(g=><span key={g.name} className="rounded-full border border-slate-700 bg-slate-900 px-2 py-1 text-xs font-bold">{g.name} <b className={+g.kd>=1?'text-emerald-300':'text-rose-300'}>{g.kd}</b></span>)}</div></td><td className="py-4 text-center font-black">{r.players}</td><td className="py-4 text-center font-black text-blue-300">{r.kills}</td><td className="py-4 text-center font-black text-pink-300">{r.deaths}</td><td className={(+r.kd>=1?'text-emerald-300':'text-rose-300')+' py-4 text-center font-black'}>{r.kd}</td><td className="py-4 pr-4 text-center"><input type="checkbox" checked={selWars.includes(String(r.id))} onClick={e=>e.stopPropagation()} onChange={e=>{let id=String(r.id);setSelDays(['all']);setSelWars(e.target.checked?[...selWars.filter(x=>x!=='all'&&x!=='current'),id]:selWars.filter(x=>x!==id));}} className="h-5 w-5 cursor-pointer accent-blue-500"/></td></tr>)}</tbody></table></div></div></Panel>}
function TopGuilds({guilds,ev}){let[sel,setSel]=useState(null),rows=[...guilds].map(g=>{let k=g.deaths,d=g.kills;return{...g,kills:k,deaths:d,kd:d?(k/d).toFixed(2):k.toFixed(2)}}).sort((a,b)=>b.kills-a.kills).slice(0,8),log=sel?ev.filter(e=>e.guild===sel.name):[];return <Panel><h3 className="text-xl font-black">🛡 Top Guilds</h3><p className="mb-4 text-xs text-slate-400">Click pe nume pentru kill log</p><table className="w-full text-sm"><thead className="text-xs uppercase text-slate-400"><tr><th className="text-left">Guild</th><th>Kills</th><th>Deaths</th><th>K/D</th></tr></thead><tbody>{rows.map((g,i)=><tr key={g.name} className="border-t border-slate-800"><td className="py-3"><button onClick={()=>setSel(g)} className="font-bold hover:text-blue-300">{i+1}. {g.name}</button></td><td className="text-center text-blue-300 font-black">{g.kills}</td><td className="text-center text-pink-300 font-black">{g.deaths}</td><td className="text-center text-emerald-300 font-black">{g.kd}</td></tr>)}</tbody></table>{sel&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4 backdrop-blur-sm"><div className="max-h-[82vh] w-full max-w-4xl overflow-hidden rounded-3xl border border-slate-700 bg-slate-950"><div className="flex justify-between border-b border-slate-800 p-5"><div><h3 className="text-2xl font-black">{sel.name} Kill Log</h3><p className="text-sm text-slate-400">{log.length} events</p></div><button onClick={()=>setSel(null)} className="rounded-xl border border-slate-700 bg-slate-900 px-4 py-2">Close</button></div><div className="max-h-[64vh] overflow-auto p-4 space-y-2">{log.map((e,i)=><div key={i} className="grid grid-cols-[82px_1fr_105px] gap-3 rounded-2xl border border-slate-800 bg-slate-900/70 p-3 text-sm"><div><b>{e.time}</b><p className="text-[10px] text-slate-500">{e.date}</p></div><p className="truncate"><b className={e.type==='kill'?'text-blue-300':'text-pink-300'}>{e.type==='kill'?e.killer:e.victim}</b> {e.type==='kill'?'killed':'died to'} <b>{e.type==='kill'?e.victim:e.killer}</b></p><span className={e.type==='kill'?'text-blue-300':'text-pink-300'}>{e.type==='kill'?'OUR KILL':'OUR DEATH'}</span></div>)}</div></div></div>}</Panel>}
function Line({data,title}){return <Panel><h2 className="text-2xl font-black">{title}</h2><div className="h-[300px]"><ResponsiveContainer><AreaChart data={data}><CartesianGrid stroke="rgba(148,163,184,.14)"/><XAxis dataKey="time" tick={{fill:'#94a3b8',fontSize:11}} angle={-35} textAnchor="end" height={55}/><YAxis tick={{fill:'#94a3b8',fontSize:11}} allowDecimals={false}/><Tooltip contentStyle={{background:'#0f172a',border:'1px solid #334155',borderRadius:14,color:'#fff'}}/><Area type="monotone" dataKey="kills" stroke="#60a5fa" fill="#60a5fa55"/><Area type="monotone" dataKey="deaths" stroke="#f9a8d4" fill="#fb718555"/></AreaChart></ResponsiveContainer></div></Panel>}

export default function App(){
let[page,setPage]=useState('nodewars'),[raw,setRaw]=useState(demo),[name,setName]=useState('Battle log'),[date,setDate]=useState(today()),[logs,setLogs]=useState([]),[members,setMembers]=useState([]),[mName,setMName]=useState(''),[selDays,setSelDays]=useState(['current']),[selWars,setSelWars]=useState(['current']),[player,setPlayer]=useState(''),[msg,setMsg]=useState(''),[calM,setCalM]=useState(mon(new Date())),[rOpen,setROpen]=useState(false),[rawM,setRawM]=useState(mon(new Date()));

useEffect(()=>{
  apiGet('/api/logs').then(setLogs).catch(()=>setLogs(rd(LK,[])));
  apiGet('/api/members').then(setMembers).catch(()=>setMembers(rd(MK,[])));
},[]);

let current=selDays.includes('current'),all=selDays.includes('all'),active=useMemo(()=>current?[{id:'current',name,date,raw}]:((all?logs:logs.filter(x=>selDays.includes(dateOf(x)))).filter(x=>selWars.includes('all')||selWars.includes(String(x.id))).map(x=>({...x,date:dateOf(x)}))),[current,all,logs,selDays,selWars,name,date,raw]),S=useMemo(()=>stats(active),[active]),label=current?'Current log':all?'All saved days':selDays[0]||'No day',dates=[...new Set(logs.map(dateOf))].sort().reverse(),marked=new Set(dates);

async function saveLog(){
  let p=parse(raw,name,date,'x');
  if(!p.length)return setMsg('Log invalid');
  try{
    let item=await apiWrite('/api/logs','POST',{name,date,raw});
    let next=[item,...logs];
    setLogs(next);
    setSelDays([item.date]);
    setSelWars([String(item.id)]);
    setMsg('Log salvat in baza de date');
  }catch(e){
    setMsg(e.message||'Eroare la salvare');
  }
}

async function addM(){
  let n=mName.trim();
  if(!n)return;
  try{
    let item=await apiWrite('/api/members','POST',{name:n});
    let next=[...members,item];
    setMembers(next);
    setMName('');
  }catch(e){
    setMsg(e.message||'Eroare la adaugare membru');
  }
}

let st=S.st,fd=S.fd,killFeeds=feed(S.ev,10,true),pStats=useMemo(()=>{if(!player)return null;let v={},kb={},mins={};S.ev.forEach(e=>{let m=tm(Math.floor(e.sec/60)*60);if(e.killer===player){add(v,e.victim);mins[m]||={time:m,kills:0,deaths:0};mins[m].kills++}if(e.victim===player){add(kb,e.killer);mins[m]||={time:m,kills:0,deaths:0};mins[m].deaths++}});let p=S.players.find(x=>x.name===player)||{kills:0,deaths:0,kd:'0.00'};return{...p,v,kb,line:S.line.map(x=>mins[x.time]||{time:x.time,kills:0,deaths:0}),ach:ach.map(a=>{let val=a[2]==='k'?p.kills:a[2]==='kd'?Number(p.kd):a[2]==='s'?(st[player]||0):a[2]==='f'?(fd[player]||0):0;return{title:a[0],goal:a[1],val,done:val>=a[1]}})}},[player,S,st,fd]);

return <main className="min-h-screen bg-[#050b16] text-slate-100"><div className="fixed inset-0 bg-[radial-gradient(circle_at_top_left,rgba(59,130,246,.18),transparent_35%)]"/><div className="relative flex gap-5 p-4"><aside className="hidden w-64 shrink-0 rounded-3xl border border-slate-700 bg-slate-950/70 p-4 xl:block"><h1 className="mb-6 text-center text-xl font-black">☾ Battle Analytics</h1>{[['nodewars','Node Wars'],['players','Player Stats'],['raw','Raw Log']].map(x=><button key={x[0]} onClick={()=>setPage(x[0])} className={`mb-2 w-full rounded-xl px-4 py-3 text-left font-bold ${((x[0]==='nodewars'&&(page==='nodewars'||page==='overview'))||page===x[0])?'border border-blue-400 bg-blue-500/20':'hover:bg-slate-900'}`}>{x[1]}</button>)}</aside><section className="min-w-0 flex-1 space-y-4">{page==='overview'&&<><div className="flex gap-2 rounded-2xl border border-slate-700 bg-slate-950/70 p-2"><button onClick={()=>setPage('nodewars')} className="rounded-xl px-4 py-2 text-sm font-bold text-slate-300 hover:bg-slate-900">Node Wars</button><button onClick={()=>setPage('overview')} className="rounded-xl border border-blue-400 bg-blue-500/20 px-4 py-2 text-sm font-bold text-white">Overview</button></div><header className="rounded-3xl border border-slate-700 bg-slate-950/70 p-5"><div className="mb-4 flex justify-between"><div><h2 className="text-2xl font-black">Battle Analytics</h2><p className="text-slate-400">{label}</p></div><button onClick={saveLog} className="rounded-xl bg-blue-600 px-4 py-2 font-bold">Save log</button></div>{msg&&<p className="mb-3 rounded-xl bg-blue-500/10 p-3 text-blue-200">{msg}</p>}<div className="grid gap-4 md:grid-cols-4"><Metric i="⚔" l="Total Kills" v={S.kills} s="Eliminations" c="border-blue-400/25 from-blue-500/20 text-blue-300"/><Metric i="☠" l="Total Deaths" v={S.deaths} s="Deaths" c="border-pink-400/25 from-pink-500/20 text-pink-300"/><Metric i="✦" l="K/D" v={S.kd} s="Ratio" c="border-violet-400/25 from-violet-500/20 text-violet-300"/><Metric i="♟" l="Players" v={S.players.length} s="Active" c="border-emerald-400/25 from-emerald-500/20 text-emerald-300"/></div></header><Line data={S.line} title="▧ Global Kill/Death Timeline"/><section className="grid gap-4 xl:grid-cols-[420px_1fr]"><Best players={S.players} members={members} st={st} fd={fd} ev={S.ev}/><Overview players={S.players} st={st} fd={fd} ev={S.ev}/></section><section className="grid gap-4 xl:grid-cols-2"><TopGuilds guilds={S.guilds} ev={S.ev}/><Panel><h3 className="mb-4 text-xl font-black">🔥 Kill Feed</h3>{killFeeds.slice(0,5).map((k,i)=><div key={i} className="mb-3 rounded-2xl border border-slate-800 bg-slate-900 p-4"><div className="flex justify-between"><b>{i+1}. {k.name}</b><b className="text-orange-300">🔥 {k.count}</b></div><p className="text-xs text-slate-400">{k.start}-{k.end} · {k.war}</p><p className="truncate text-xs text-slate-500">{k.victims.join(', ')}</p></div>)}</Panel></section></>}{page==='players'&&<Panel><h2 className="mb-4 text-2xl font-black">Player Stats</h2><select value={player} onChange={e=>setPlayer(e.target.value)} className="mb-4 w-full rounded-xl border border-slate-700 bg-slate-950 p-3"><option value="">Select player</option>{S.players.map(p=><option key={p.name}>{p.name}</option>)}</select>{pStats&&<><div className="grid gap-4 md:grid-cols-3"><Metric i="⚔" l="Kills" v={pStats.kills} s={player} c="border-blue-400/25 from-blue-500/20 text-blue-300"/><Metric i="☠" l="Deaths" v={pStats.deaths} s="Deaths" c="border-pink-400/25 from-pink-500/20 text-pink-300"/><Metric i="✦" l="K/D" v={pStats.kd} s="Ratio" c="border-violet-400/25 from-violet-500/20 text-violet-300"/></div><Line data={pStats.line} title="Player Kill/Death Timeline"/><div className="grid gap-4 md:grid-cols-2"><RankList title="Favourite Targets" items={Object.entries(pStats.v).map(([name,kills])=>({name,kills})).sort((a,b)=>b.kills-a.kills)} valueKey="kills"/><RankList title="Killed By" items={Object.entries(pStats.kb).map(([name,kills])=>({name,kills})).sort((a,b)=>b.kills-a.kills)} valueKey="kills"/></div><Panel><h3 className="mb-4 text-xl font-black">✦ Achievements</h3><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-4">{pStats.ach.map(a=><div key={a.title} className={(a.done?'border-emerald-400 bg-emerald-500/10':'border-slate-800 bg-slate-950/40')+' rounded-xl border p-3'}><p className="font-bold">{a.done?'✅':'🔒'} {a.title}</p><div className="mt-2 h-2 rounded-full bg-slate-800"><div className={(a.done?'bg-emerald-400':'bg-blue-500')+' h-2 rounded-full'} style={{width:Math.min(100,a.val/a.goal*100)+'%'}}/></div><p className="mt-1 text-xs text-slate-500">{Number(a.val).toFixed(a.goal<=10?2:0)} / {a.goal}</p></div>)}</div></Panel></>}</Panel>}{page==='nodewars'&&<><div className="flex gap-2 rounded-2xl border border-slate-700 bg-slate-950/70 p-2"><button onClick={()=>setPage('nodewars')} className="rounded-xl border border-blue-400 bg-blue-500/20 px-4 py-2 text-sm font-bold text-white">Node Wars</button><button onClick={()=>setPage('overview')} className="rounded-xl px-4 py-2 text-sm font-bold text-slate-300 hover:bg-slate-900">Overview</button></div><NodeWars logs={logs} setPage={setPage} setSelDays={setSelDays} setSelWars={setSelWars} selWars={selWars}/></>}{page==='raw'&&<div className="grid gap-4 xl:grid-cols-[1fr_380px]"><Panel><h2 className="mb-4 text-2xl font-black">Raw Log</h2><div className="mb-3 grid gap-3 md:grid-cols-[1fr_190px_100px]"><input value={name} onChange={e=>setName(e.target.value)} className="rounded-xl border border-slate-700 bg-slate-900 p-3"/><div className="relative"><button type="button" onClick={()=>setROpen(!rOpen)} className="w-full rounded-xl border border-blue-500/30 bg-blue-500/10 p-3 text-left hover:bg-blue-500/20"><span className="block text-xs font-bold text-blue-200">War date</span><span className="font-black">{date}</span></button>{rOpen&&<div className="absolute left-0 right-0 z-40 mt-2"><Cal m={rawM} setM={setRawM} selected={date} marked={marked} onPick={d=>{setDate(d);setROpen(false)}} footer={<div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>{setDate(today());setROpen(false)}} className="rounded-xl border border-slate-700 px-2 py-2 text-xs font-bold">Today</button><button onClick={()=>setROpen(false)} className="rounded-xl border border-slate-700 px-2 py-2 text-xs font-bold">Close</button></div>}/></div>}</div><button onClick={saveLog} className="rounded-xl bg-blue-600 font-bold">Save</button></div>{msg&&<p className="mb-3 rounded-xl bg-blue-500/10 p-3 text-blue-200">{msg}</p>}<textarea value={raw} onChange={e=>setRaw(e.target.value)} className="h-96 w-full rounded-2xl border border-slate-700 bg-slate-950 p-4 font-mono text-sm"/></Panel><Panel><h2 className="mb-4 text-2xl font-black">History</h2>{logs.map(x=><div key={x.id} className="mb-3 rounded-xl bg-slate-900 p-3"><b>{x.name}</b><p className="text-xs text-slate-500">{dateOf(x)}</p><div className="mt-2 flex gap-2"><button onClick={()=>{setRaw(x.raw);setName(x.name);setDate(dateOf(x));}} className="rounded-lg bg-blue-600 px-3 py-1 text-xs">Load</button><button onClick={async()=>{if(!confirm('Stergi acest log din baza de date?'))return;try{await apiWrite('/api/logs/'+x.id,'DELETE');let n=logs.filter(l=>l.id!==x.id);setLogs(n);setMsg('Log sters din baza de date')}catch(e){setMsg(e.message||'Eroare la stergere')}}} className="rounded-lg bg-rose-600 px-3 py-1 text-xs">Delete</button></div></div>)}</Panel></div>}</section></div></main>
}
